# Environment variables for Yandex Disk Menu (Python)
# This file contains configuration that can be sourced by both shell and Python versions

# Application version
YADISK_MENU_VERSION=1.7.0

# Default paths (can be overridden by setup scripts)
# YA_DISK_ROOT will be set by setup scripts based on user configuration
# These are fallback values if environment is not properly configured
DEFAULT_YA_DISK_ROOT="$HOME/Public"
DEFAULT_YA_DISK_RELATIVE="yaDisk"
DEFAULT_INBOX_RELATIVE="Media"